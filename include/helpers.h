#include<fstream>
#include<iostream>
#include<string>
#include<ctime>
#include<chrono>
using namespace std;

namespace helpers {
    bool isAsciiNumber(char c);
    bool isValidFilePath(const string & filepath);
}
